# 실습 가이드 스타일 규칙

`배포자료/실습가이드.html` 한 파일에만 적용됩니다.
값은 전부 `:root`의 토큰으로 잡혀 있으니, 고칠 때는 개별 규칙이 아니라 토큰을 고칩니다.

---

## 1. 간격 — 가까우면 한 덩어리, 멀면 다른 맥락

읽는 사람이 선을 긋지 않아도 묶음이 보이게 하는 것이 목적입니다.
네 단계뿐이고, 중간값은 쓰지 않습니다.

| 토큰 | 값 | 언제 |
|---|---|---|
| `--gap-tight` | 10px | 설명 문단과 그 문단이 가리키는 것 |
| `--gap-pair` | 14px | 한 묶음 안에서 이어지는 요소 |
| `--gap-block` | 30px | 묶음과 묶음 사이 |
| `--gap-topic` | 50px | 새 소주제 (`h3` 위) |

체크 항목끼리는 8px, 메모 칸끼리와 메모 뒤 체크는 9px로 더 붙입니다.
여러 개가 한 목록으로 읽혀야 하기 때문입니다.
챕터 사이는 `--sp-52`(52px)입니다.

### tight — 설명과 대상은 붙인다

문단이 바로 아래 것을 소개하는 경우입니다. 붙어 있어야 "이걸 말하는 거구나"가 보입니다.

```
h3 소제목
  ↓ 10px
설명 문단
  ↓ 10px
복사 카드 / 표 / 박스 / 체크
```

### pair — 한 묶음 안

같이 움직이는 것들입니다.

- 복사 카드 → 그 카드로 한 일을 확인하는 체크
- 메모 칸 → 메모 칸, 그리고 마지막 메모 → 체크
- 점수 구간 안내 → 점수 입력칸
- 표 → 그 표를 해설하는 박스
- 이어지는 같은 종류의 박스

### block — 묶음이 끝났다

체크 목록이 끝나고 다음 것이 시작될 때, 서로 관계없는 박스 사이입니다.

### topic — 새 소주제

`h3`가 나오면 무조건 위로 50px입니다. 예외를 두지 않습니다.

### 구현 방식

관계를 CSS 인접 선택자로 표현합니다. HTML에 클래스를 더 붙이지 않습니다.

```css
.step > p:has(+ .snip){margin-bottom:var(--gap-tight);}   /* 설명 → 카드 */
.snip:has(+ .task){margin-bottom:var(--gap-pair);}        /* 카드 → 체크 */
.task:not(:has(+ .task)){margin-bottom:var(--gap-block);} /* 체크 묶음 끝 */
```

두 가지를 주의합니다.

- **트랙에 따라 숨는 형제**가 사이에 끼면 `:has()`가 어긋납니다.
  그럴 때는 앞 요소의 `margin-bottom`이 아니라 **뒤 요소의 `margin-top`**에 겁니다.
  `.made + .task`, `.pickfirst + .made`가 그렇게 되어 있습니다.
- 표 위에는 스크립트가 좌우 스크롤 안내(`.swipe`)를 끼워 넣습니다.
  `.step > p:has(+ .swipe)`로 같이 받아둡니다.

`:has()`를 못 읽는 브라우저에서는 전부 `block` 간격으로 보입니다. 묶임은 사라져도 읽는 데 지장은 없습니다.

---

## 2. 박스 안 줄글 — 박스가 무엇이든 한 벌

`.note` `.howto` `.dl` `.tree` `.pickfirst` `details .in` 전부 같은 활자로 씁니다.

| | 값 |
|---|---|
| 크기 | `--text-small` 14.5px |
| 행간 | `--leading-box` 1.58 |
| 문단 사이 | `--gap-inbox` 10px |
| 마지막 문단 | `margin-bottom:0` |
| 박스 제목(`b.t`) 아래 | 10px |
| 목록 항목 사이 | 7px |

본문(`.step > p`)은 16px / 1.72입니다. 박스는 더 작고 더 촘촘해서, 활자만으로도 "본문에서 잠깐 빠져나온 덩어리"로 읽힙니다.

**색은 역할에 따라 다릅니다.** 이건 통일 대상이 아닙니다.

| 색 | 쓰는 곳 |
|---|---|
| `--ink` | `.note` — 본문과 같은 무게로 읽어야 하는 강조 |
| `--ink-2` | `details .in`, `.hint`, `.trackcard p` — 보조 설명 |
| `--ink-3` | `.tree .tail` 같은 꼬리말, 마이크로 라벨 |

### 박스 제목

`<b class="t">` 한 줄입니다. 모노 10.5px 대문자, 자간 `--tracking-label`.
문장이 아니라 이름표이므로 마침표를 찍지 않습니다.

---

## 3. 박스를 고를 때

| 클래스 | 무엇 | 생김새 |
|---|---|---|
| `.note` | 일반 설명 | 왼쪽 3px 강조선 |
| `.note.go` | 넘어가기 전 정리 | 강조선이 진한 색 |
| `.note.stop` | 하지 말 것 · 주의 | 강조선이 경고색 |
| `.dl` | 내려받을 것 | 1.5px 실선 테두리 |
| `.tree` | 폴더 구조 | 1px 실선 테두리 |
| `.pickfirst` | 트랙을 안 고른 상태의 자리표시 | 점선 테두리 |
| `.snip` | 복사해서 쓰는 프롬프트 | 상단 라벨 + 복사 버튼 |

한 챕터에 `.note.go`는 하나입니다. 마무리 자리에만 씁니다.

---

## 4. 앞 챕터에서 넘어온 내용 — `.carry`

참여자가 적은 것은 그 챕터에서 끝나지 않습니다. 필요한 뒤 챕터에서 다시 보여줍니다.

```html
<div class="carry"
     data-carry="memoFail"
     data-title="3 EVAL에서 받은 문장"
     data-empty="3 Eval에서 “다음에 쓸 문장”을 붙여넣으면 여기에 나옵니다"></div>
```

빈 `div` 하나만 두면 됩니다. 라벨·복사 버튼·본문은 스크립트가 만듭니다.

| 속성 | 뜻 |
|---|---|
| `data-carry` | 가져올 입력칸의 `data-k`. 쉼표로 여러 개 |
| `data-title` | 카드 제목. 어느 챕터에서 온 것인지 밝힙니다 |
| `data-empty` | 아직 비었을 때 보여줄 안내. 어디서 채우는지 알려줍니다 |

- 값이 하나면 그대로, 여럿이면 항목 이름을 위에 붙여 보여줍니다.
- 점수(`sv1` `sv2` `svm`)는 화면에 `점`을 붙이고, 복사할 때는 숫자만 담습니다.
- 비어 있으면 점선 테두리에 흐린 글씨, 복사 버튼은 숨습니다.
- 입력하는 즉시 갱신됩니다. `paint()`가 `paintCarry()`를 부릅니다.

새 항목을 넘기려면 `CARRY_LABEL`에 표시 이름 한 줄을 더합니다.

### 지금 이어져 있는 흐름

```
2 실행  이상해 보였던 것 ─────────→ 3 Eval   채점 결과와 대조
3 Eval  1차 점수 ────────────────→ 4 수정   2차 점수 바로 위
3 Eval  다음에 쓸 문장 ───────────→ 4 수정   내 문장을 쓰기 전에
3 Eval + 4 수정  규칙 문장 ───────→ 5 자산화 스킬 절차의 재료
6 내 업무 + 점수 셋 ──────────────→ 7 발표   발표 세 문장의 재료
6 내 업무  무엇을 · 통과 기준 ────→ 8 마무리 다음 주 계획
```

---

## 5. 어투

`~하세요` `~해주세요` `~하면 됩니다`가 아니라 **`~합니다`**로 씁니다.
세 군데만 예외입니다.

- `손을 들어주세요` — 그 자리에서 사람에게 하는 부탁
- `<pre>` 안의 복사용 프롬프트 — Claude에게 주는 지시라 명령형이 맞습니다
- 마무리 챕터의 "가져갈 문장 셋" — 표어라 `~한다` 체

---

## 6. 고친 뒤에 반드시

폰트가 본문에 쓰인 글자만큼만 잘려서 들어가 있습니다. 새 글자를 쓰면 그 글자만 다른 폰트로 나옵니다.

```bash
bash 폰트원본/서브셋_다시만들기.sh
bash 배포.sh "한국어 커밋 메시지"
```
